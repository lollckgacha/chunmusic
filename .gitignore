# 관리자 전용 파일 - Firebase 데이터베이스 시크릿이 포함되어 있어 공개 저장소에 올리면 안 됨.
# Google Apps Script 에디터에 직접 붙여넣어 쓰는 파일이라 배포에도 필요 없음.
apps-script-songlist.gs.txt

# 내부 기획 문서 (스트리머 플레이용 배포와 무관)
claude-code-prompt.md

# 로컬 개발 서버 설정
.claude/

# SOOP 채팅 릴레이 서버 - 실제 시크릿은 .env에만 넣고 여기서 제외 (.env.example은 추적함)
soop-chat-relay/.env
soop-chat-relay/node_modules/
